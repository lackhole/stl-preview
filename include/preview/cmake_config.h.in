//
// Created by yonggyulee on 2024. 7. 19.
//


#ifdef PREVIEW_CMAKE_CONFIG

#cmakedefine01 PREVIEW_HAVE_STRING_VIEW
#cmakedefine01 PREVIEW_HAVE_CONTIGUOUS_ITERATOR_TAG

#else

#include "preview/core.h"

#define PREVIEW_HAVE_STRING_VIEW             (PREVIEW_CXX_VERSION >= 17)

# if PREVIEW_CXX_VERSION >= 20 && (PREVIEW_ANDROID == 0 || (defined(PREVIEW_NDK_VERSION_MAJOR) && PREVIEW_NDK_VERSION_MAJOR >= 26))
#   define PREVIEW_HAVE_CONTIGUOUS_ITERATOR_TAG 1
# else
#   define PREVIEW_HAVE_CONTIGUOUS_ITERATOR_TAG 0
# endif

#endif
